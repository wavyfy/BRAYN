{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const j=JSON.parse(d);const fp=(j.tool_input&&j.tool_input.file_path)||'';const pats=[/(^|[\\\\\\/])\\.env(\\..*)?$/i,/credentials\\.json$/i,/secret/i,/\\.pem$/i,/\\.key$/i,/id_rsa/i,/id_ed25519/i];if(pats.some(p=>p.test(fp))){console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'PreToolUse',permissionDecision:'deny',permissionDecisionReason:'Blocked: sensitive/secret file ('+fp+'). CLAUDE.md security rule: never put secrets in source, never expose secrets.'}}));}});\""
          }
        ]
      }
    ]
  }
}
